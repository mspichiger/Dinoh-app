{
    "name": "dinoh-backend",
    "version": "0.1.0",
    "private": true,
    "description": "Backend API for the DINOH Digital Innovation Hub",
    "main": "dist/server.js",
    "scripts": {
        "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
        "build": "tsc -p tsconfig.json",
        "start": "node dist/server.js",
        "lint": "tsc --noEmit",
        "db:migrate": "ts-node-dev --transpile-only --no-notify --no-deps src/db/migrate.ts"
    },
    "dependencies": {
        "cors": "^2.8.5",
        "dotenv": "^17.4.2",
        "express": "^5.1.0",
        "google-auth-library": "^9.15.1",
        "pg": "^8.21.0"
    },
    "devDependencies": {
        "@types/cors": "^2.8.17",
        "@types/express": "^5.0.1",
        "@types/node": "^20.17.19",
        "@types/pg": "^8.20.0",
        "ts-node-dev": "^2.0.0",
        "typescript": "~5.9.2"
    }
}
